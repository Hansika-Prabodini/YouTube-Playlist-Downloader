# Example environment variables for local development
# NOTE: Do not commit real secrets. Copy this file to `.env` and edit locally.

# OpenAI API key required for Taipy chat demo
# OPENAI_API_KEY=your_key_here

# Taipy runtime flags (safe defaults are OFF). Keep both OFF in production.
# Accepted true values: 1, true, yes, on (case-insensitive)
# Accepted false values: 0, false, no, off (case-insensitive)
# TAIPY_DEBUG=false
# TAIPY_RELOAD=false
